query CurationSession($id: ID!){
  curationSession(id: $id) {
    id
    project{
      id
    }
    import {
      id
    }
    dataType{
      id
      dataAttributes {
        id
        name
      }
    }

    curatableRecords {
      id
      data
    }
  }
}
