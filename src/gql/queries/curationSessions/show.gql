query CurationSession($id: ID!){
  curationSession(id: $id) {
    id
    mapping
    project{
      id
    }
    import {
      id
    }
    dataType{
      id
      dataAttributes {
        id
        name
        attributeType
      }
    }

    curatableRecords {
      id
      data
    }
  }
}
